import { Routes, Route } from "react-router-dom";
import './App.css';

export function App() {
    return (
        <div className="app">
            <Routes>
                <Route path={} element={} />
            </Routes>
        </div>
    )
}